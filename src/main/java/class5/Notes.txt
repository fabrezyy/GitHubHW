Scanner class:
 Scanner is a class that helps us take input data from the user from keyboard. Just like scanner class we have so many
 other classes that we can use from Java and other libraries.
 These classes are divided into methods each method can do something useful. Like nextInt(). nextBoolean().
Logical operators:
 Logical operators are used to combine multiple conditions. There are 3 logical operators in java.
 1)Logical NOT "!"
 2)Logical  OR "||"
 3)Logical AND "&&"
Switch statements:


Important note:
 Every class in java also acts as a data type. We can create variables of these classes. If we want to reuse some codes
 from a class. We can create its objects, then we can start calling those methods.